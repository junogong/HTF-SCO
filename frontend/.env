# Backend API URL (for local development, Vite proxies to this)
# When deploying the compiled frontend, this won't be used by Vite's proxy, 
# but could be used in client.js if you choose to bake the URL in at build time.
VITE_API_URL=http://localhost:5000
